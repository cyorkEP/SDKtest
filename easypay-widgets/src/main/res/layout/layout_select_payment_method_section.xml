<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:background="@color/easypay_widget_sheet_background">

    <TextView
        android:id="@+id/tv_title"
        style="@style/Text.Body.3.SemiBold"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="@dimen/easypay_manage_cards_sheet_default_horizontal_padding"
        android:textColor="@color/easypay_widget_text_foreground"
        app:layout_constraintBottom_toBottomOf="@id/btn_close"
        app:layout_constraintEnd_toStartOf="@id/btn_close"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/btn_close"
        tools:text="@string/select_payment_method" />

    <ImageButton
        android:id="@+id/btn_close"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:layout_marginEnd="@dimen/easypay_manage_cards_sheet_default_horizontal_padding"
        android:background="@android:color/transparent"
        android:contentDescription="@string/close_manage_cards_sheet"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_exit_circle_outlined"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>